
const initState: StateProps = {
  images: [],
  uploadedImages: [],
  webId: null,
}

export default function (
  action: Actions,
  state: StateProps = initState
) {
  switch (action.type) {
    default:
      return state
  }
}
